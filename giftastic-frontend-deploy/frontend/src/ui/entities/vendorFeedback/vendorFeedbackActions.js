// The backend currently authorizes these mutations with a read permission.
// Phase 1 deliberately withholds unsafe moderation actions until that contract is corrected.
export const buildVendorFeedbackActions = () => [];
